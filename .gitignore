.venv/
__pycache__/
*.egg-info/
.pytest_cache/
dist/

# compiled artifacts (adapters + manifests) are user data, never tracked
artifacts/

# labeled datasets (regenerable from teacher; may be large)
data/
case-study/*/work/
.env

# machine-/user-specific layers (see CLAUDE.md): never published
CLAUDE.local.md
local/
.claude/
.smallbatch-plans/

# internal review documents — never ship these
PROJECT_EVALUATION.md
V0.2_RELEASE_PLAN_REVIEW.md
V0.2_RELEASE_PLAN_READINESS_REVIEW.md

# internal operational docs — kept locally, not tracked or shipped
RELEASE_CHECKLIST.md
ROADMAP.md
